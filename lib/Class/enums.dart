enum coaches_qualification_enum {
  None,
  Grassroots,
  Community,
  CLicence,
  BLicence,
  ALicence,
  ProLicence
}

enum coaches_verification_status_enum { pending, verified, denied }

enum job_offers_status_enum { pending, accepted, declined }

enum users_gender_enum { male, female, other, prefer_not_to_say }

enum users_notification_preference_enum { email, text }
